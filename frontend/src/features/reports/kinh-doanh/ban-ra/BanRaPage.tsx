export default function BanRaPage() {
  // TODO: Migrate from templates/baocao_banra/baocao_banra.html
  // Key features:
  // - Flat table (not tree-based) — uses DataTable not TreeTable
  // - Autocomplete filter bar: KH, Sản phẩm, Kho, BP, NVKD
  // - Column visibility panel (show/hide columns)
  // - Per-column filter dropdown (unique values, checkbox multi-select)
  // - Column resize
  // - Export Excel (visible columns + filtered data)
  // - Large dataset (50k+ rows) — needs virtualization
  return (
    <div className="flex h-full items-center justify-center text-surface-4">
      <p>TODO: Migrate Báo cáo Bán Ra</p>
    </div>
  )
}
